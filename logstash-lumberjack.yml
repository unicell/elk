logstash:
  extends:
    file: common.yml
    service: logstash
  ports:
    - "5043:5043"
  extra_hosts:
    - "elasticsearch: ${ES_HOST}"
  command: logstash -f /etc/logstash/conf.d/twitter-pipeline.conf
